server:  port: 8080spring:  datasource:    url: jdbc:mysql://localhost:3306/mun?useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&serverTimezone=GMT%2B8    username: root    password: 123456    driver-class-name: com.mysql.cj.jdbc.Driver  mvc:    pathmatch:      matching-strategy: ant_path_matcher  batch:    job:      enabled=false  hikari:    pool-name: DateHikariC    minimux-idle: 5    idel-timeout: 18000    maximum-pool-size: 10    auto-commit: true    max-lifetime: 1800000    connection-timeout: 30000    connection-test-query: SELECT 1# jwt自定义配置jwt:  # JWT存储的请求头  tokenHeader: token  # JWT加解密使用的密钥  secret: secret  # JWT的过期限时间(60*60*24*7)  expiration: 360000mybatis:  mapper-locations: classpath:mapper/*.xml    # mapper映射文件位置  configuration:    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   #用于控制台打印sql语句